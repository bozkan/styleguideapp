<div id="notices">
  <% flash.each do |key, value| %>
    <div class="alert alert-snackbar bg-<%= key %> box-shadow--xlg">
      <p class="text-white mb-0"><%= value %></p>
    </div>
  <% end %>
</div>
